import Link from "next/link";

const FEATURES = [
  {
    title: "Grounded answers",
    body: "Every response is pulled from indexed invoice sections, then returned with the evidence that produced it.",
    detail: "RAG + source chunks",
  },
  {
    title: "Visual audit trail",
    body: "The PDF stays beside the conversation, so a teammate can click a source and inspect the exact region on the page.",
    detail: "page + bounding box",
  },
  {
    title: "Payment matching",
    body: "Bank CSVs are compared against extracted invoice amounts to flag paid, partial, and unpaid records.",
    detail: "CSV reconciliation",
  },
  {
    title: "Local working state",
    body: "Uploads and ChromaDB state stay local during the prototype, which keeps the makethon demo simple to run.",
    detail: "FastAPI + Chroma",
  },
];

const FLOW = [
  ["01", "Upload the invoice", "PyMuPDF extracts text blocks, section labels, and PDF-space bounding boxes."],
  ["02", "Ask the document", "Gemini answers from retrieved chunks and refuses questions outside the invoice."],
  ["03", "Inspect the proof", "Source chips jump the reviewer back to the page region that supports the answer."],
  ["04", "Reconcile payments", "A bank statement CSV marks invoice amounts as paid, partial, or unpaid."],
];

const SIGNALS = [
  ["12", "sections indexed from a typical invoice"],
  ["5", "source chunks retrieved per answer"],
  ["10s", "metrics refresh interval"],
];

function LogoMark() {
  return (
    <div className="grid h-8 w-8 place-items-center rounded-md bg-accent text-ink shadow-glow">
      <span className="font-mono text-sm font-black">I</span>
    </div>
  );
}

function EvidenceScene() {
  return (
    <div
      aria-hidden="true"
      className="pointer-events-none absolute inset-x-0 bottom-0 top-12 overflow-hidden"
    >
      <div className="absolute inset-0 bg-[linear-gradient(rgba(246,241,230,0.08)_1px,transparent_1px),linear-gradient(90deg,rgba(246,241,230,0.06)_1px,transparent_1px)] bg-[size:72px_72px]" />
      <div className="absolute inset-0 bg-[linear-gradient(180deg,rgba(17,20,15,0.12)_0%,rgba(17,20,15,0.72)_62%,#11140f_100%)]" />

      <div className="absolute left-[6vw] top-28 hidden w-[21rem] rotate-[-5deg] rounded-lg border border-card-border bg-paper p-5 text-ink shadow-[0_30px_90px_rgba(0,0,0,0.28)] md:block">
        <div className="mb-5 flex items-start justify-between">
          <div>
            <div className="h-2 w-16 rounded-full bg-ink/70" />
            <div className="mt-2 h-2 w-24 rounded-full bg-ink/25" />
          </div>
          <div className="font-mono text-[10px] uppercase text-ink/50">INV-2048</div>
        </div>
        <div className="space-y-2">
          <div className="grid grid-cols-[1fr_56px] gap-3">
            <div className="h-3 rounded-full bg-ink/20" />
            <div className="h-3 rounded-full bg-ink/20" />
          </div>
          <div className="grid grid-cols-[1fr_56px] gap-3">
            <div className="h-3 rounded-full bg-ink/10" />
            <div className="h-3 rounded-full bg-ink/10" />
          </div>
          <div className="grid grid-cols-[1fr_56px] gap-3">
            <div className="h-3 rounded-full bg-ink/10" />
            <div className="h-3 rounded-full bg-ink/10" />
          </div>
        </div>
        <div className="mt-5 rounded-md border-2 border-accent bg-accent/20 px-3 py-2">
          <div className="mb-1 h-2 w-20 rounded-full bg-accent/80" />
          <div className="h-3 w-full rounded-full bg-ink/25" />
        </div>
        <div className="mt-5 ml-auto h-3 w-28 rounded-full bg-ink/40" />
      </div>

      <div className="surface-inset absolute right-[-3rem] top-24 hidden w-[44rem] rotate-[2deg] overflow-hidden rounded-xl border border-card-border bg-sidebar shadow-glow lg:block">
        <div className="flex items-center gap-2 border-b border-card-border bg-card px-4 py-3">
          <div className="h-2.5 w-2.5 rounded-full bg-accent" />
          <div className="h-2.5 w-2.5 rounded-full bg-ember" />
          <div className="h-2.5 w-2.5 rounded-full bg-moss" />
          <div className="ml-4 h-2 w-40 rounded-full bg-text-secondary/20" />
        </div>
        <div className="grid h-[27rem] grid-cols-[1.05fr_0.95fr]">
          <div className="border-r border-card-border p-5">
            <div className="mb-5 inline-flex rounded-md bg-accent px-3 py-1.5 text-xs font-semibold text-ink">
              grounded
            </div>
            <div className="ml-auto mb-4 w-72 rounded-xl rounded-br-sm bg-accent px-4 py-3 text-sm font-medium text-ink">
              What is the final amount due?
            </div>
            <div className="w-80 rounded-xl rounded-bl-sm border border-card-border bg-background px-4 py-3 text-sm text-text-primary">
              <p>The total amount due is EUR 4,250.00.</p>
              <div className="mt-3 flex gap-2">
                <span className="rounded-md border border-accent/40 bg-accent/20 px-2 py-1 font-mono text-[11px] text-accent">
                  p.1 totals
                </span>
                <span className="rounded-md border border-card-border bg-card px-2 py-1 font-mono text-[11px] text-text-secondary">
                  score 0.86
                </span>
              </div>
            </div>
            <div className="mt-8 grid grid-cols-3 gap-3">
              {SIGNALS.map(([value, label]) => (
                <div key={value} className="rounded-lg border border-card-border bg-card p-3">
                  <div className="font-mono text-xl font-semibold text-text-primary">{value}</div>
                  <div className="mt-1 text-[11px] leading-snug text-text-secondary">{label}</div>
                </div>
              ))}
            </div>
          </div>
          <div className="bg-background/70 p-5">
            <div className="mx-auto h-full max-w-[15rem] rounded-md bg-paper p-4 text-ink shadow-[0_20px_60px_rgba(0,0,0,0.28)]">
              <div className="h-2 w-24 rounded-full bg-ink/50" />
              <div className="mt-2 h-2 w-16 rounded-full bg-ink/20" />
              <div className="my-5 h-px bg-ink/10" />
              <div className="space-y-2">
                <div className="h-2 rounded-full bg-ink/20" />
                <div className="h-2 rounded-full bg-ink/20" />
                <div className="h-2 w-3/4 rounded-full bg-ink/20" />
              </div>
              <div className="mt-8 rounded border-2 border-accent bg-accent/20 p-2">
                <div className="h-2 rounded-full bg-accent/70" />
              </div>
              <div className="mt-3 h-2 w-20 rounded-full bg-ink/20" />
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

export default function LandingPage() {
  return (
    <main id="main-content" className="min-h-screen overflow-hidden bg-background text-text-primary">
      <nav className="fixed inset-x-0 top-0 z-50 border-b border-card-border/70 bg-background/90 backdrop-blur-md">
        <div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 sm:px-8">
          <Link href="/" className="pressable focus-ring flex items-center gap-3 rounded-md">
            <LogoMark />
            <span className="font-semibold">INFORM</span>
          </Link>

          <div className="hidden items-center gap-7 text-sm text-text-secondary md:flex">
            <a className="pressable focus-ring rounded-sm hover:text-text-primary" href="#proof">Proof</a>
            <a className="pressable focus-ring rounded-sm hover:text-text-primary" href="#workflow">Workflow</a>
            <a className="pressable focus-ring rounded-sm hover:text-text-primary" href="#demo">Demo path</a>
          </div>

          <Link
            href="/login"
            className="pressable focus-ring rounded-md bg-paper px-4 py-2 text-sm font-semibold text-ink hover:bg-ember"
          >
            Sign in
          </Link>
        </div>
      </nav>

      <section className="relative min-h-[94dvh] px-5 pt-28 sm:px-8">
        <EvidenceScene />

        <div className="relative z-10 mx-auto flex min-h-[calc(94dvh-7rem)] max-w-7xl flex-col justify-between">
          <div className="max-w-5xl pt-12 sm:pt-20">
            <p className="reveal-in mb-8 max-w-xl text-sm font-semibold uppercase text-ember">
              Invoice intelligence for teams that need the receipt
            </p>
            <h1 className="reveal-in text-balance max-w-6xl text-7xl font-black leading-[0.78] text-paper sm:text-9xl lg:text-[10rem] xl:text-[11rem]">
              INFORM
            </h1>
            <p className="reveal-in mt-8 max-w-2xl text-pretty text-lg leading-8 text-text-secondary sm:text-xl">
              Ask invoices questions, inspect the exact source region, and reconcile bank statements without losing the audit trail.
            </p>
            <div className="reveal-in mt-10 flex flex-col gap-3 sm:flex-row">
              <Link
                href="/login"
                className="pressable focus-ring inline-flex items-center justify-center rounded-md bg-accent px-6 py-3 text-sm font-black text-ink hover:bg-accent-hover"
              >
                Open workspace
              </Link>
              <a
                href="#proof"
                className="pressable focus-ring inline-flex items-center justify-center rounded-md border border-card-border bg-card/80 px-6 py-3 text-sm font-semibold text-text-primary hover:border-accent/70"
              >
                See the proof loop
              </a>
            </div>
          </div>

          <div className="relative z-10 mt-16 grid border-t border-card-border/80 py-5 text-sm text-text-secondary sm:grid-cols-3">
            {SIGNALS.map(([value, label]) => (
              <div key={value} className="flex items-baseline gap-3 py-2">
                <span className="font-mono text-2xl font-semibold text-paper">{value}</span>
                <span className="max-w-48 leading-snug">{label}</span>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section id="proof" className="relative border-t border-card-border/70 px-5 py-24 sm:px-8 lg:py-32">
        <div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-[0.8fr_1.2fr]">
          <div>
            <h2 className="text-balance text-4xl font-black leading-tight text-paper sm:text-6xl">
              The answer never travels alone.
            </h2>
            <p className="mt-6 max-w-[58ch] text-pretty text-base leading-8 text-text-secondary">
              INFORM is shaped around one review loop: answer, source, page region. The design keeps conversation and document evidence in the same field of view.
            </p>
          </div>

          <div className="grid grid-flow-dense grid-cols-1 gap-4 md:grid-cols-6">
            {FEATURES.map((feature, index) => (
              <article
                key={feature.title}
                className={`hover-lift pressable surface-inset rounded-xl border border-card-border bg-card p-6 ${
                  index === 0 ? "md:col-span-4 md:row-span-2" : "md:col-span-2"
                } ${index === 3 ? "md:col-span-4" : ""}`}
              >
                <p className="mb-8 font-mono text-xs text-ember">{feature.detail}</p>
                <h3 className="text-2xl font-bold text-text-primary">{feature.title}</h3>
                <p className="mt-4 text-pretty text-sm leading-7 text-text-secondary">{feature.body}</p>
              </article>
            ))}
          </div>
        </div>
      </section>

      <section id="workflow" className="border-t border-card-border/70 px-5 py-24 sm:px-8 lg:py-32">
        <div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-[22rem_1fr]">
          <div className="lg:sticky lg:top-28 lg:h-fit">
            <h2 className="text-balance text-4xl font-black leading-tight text-paper">
              From PDF to payment status.
            </h2>
            <p className="mt-5 text-pretty leading-7 text-text-secondary">
              Four steps, each leaving enough context for another teammate to check your work.
            </p>
          </div>

          <div className="divide-y divide-card-border/80 border-y border-card-border/80">
            {FLOW.map(([step, title, body]) => (
              <div key={step} className="grid gap-5 py-8 sm:grid-cols-[7rem_1fr]">
                <span className="font-mono text-4xl font-semibold text-accent">{step}</span>
                <div>
                  <h3 className="text-2xl font-bold text-text-primary">{title}</h3>
                  <p className="mt-2 max-w-[62ch] text-pretty leading-7 text-text-secondary">{body}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section id="demo" className="px-5 pb-20 sm:px-8">
        <div className="surface-inset mx-auto grid max-w-7xl gap-8 overflow-hidden rounded-2xl border border-card-border bg-paper p-6 text-ink sm:p-10 lg:grid-cols-[1fr_22rem]">
          <div>
            <h2 className="text-balance text-4xl font-black leading-tight sm:text-6xl">
              Ready for the makethon demo.
            </h2>
            <p className="mt-5 max-w-[62ch] text-pretty text-base leading-8 text-ink/70">
              Sign in, upload an invoice, ask for the amount due, click the source, then run a bank CSV through reconciliation.
            </p>
          </div>
          <div className="flex items-end lg:justify-end">
            <Link
              href="/login"
              className="pressable focus-ring inline-flex w-full items-center justify-center rounded-md bg-ink px-6 py-4 text-sm font-black text-paper hover:bg-accent hover:text-ink sm:w-auto"
            >
              Start the walkthrough
            </Link>
          </div>
        </div>
      </section>

      <footer className="border-t border-card-border px-5 py-8 text-sm text-text-secondary sm:px-8">
        <div className="mx-auto flex max-w-7xl flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
          <div className="flex items-center gap-3">
            <LogoMark />
            <span>INFORM | Merge Conflicts | Makethon 2026</span>
          </div>
          <Link className="pressable focus-ring rounded-sm hover:text-text-primary" href="/login">
            Go to workspace
          </Link>
        </div>
      </footer>
    </main>
  );
}
